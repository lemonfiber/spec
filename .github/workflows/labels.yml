# Keep this repo's labels in sync with the canonical set in the spec (OPS-R12).
name: labels
on:
  workflow_dispatch:
  schedule:
    - cron: "0 4 * * 1"
permissions:
  contents: read
jobs:
  sync:
    permissions:
      contents: read
      issues: write
    uses: lemonfiber/spec/.github/workflows/label-sync.yml@775ae5558961aee92d99ea047f1e0d55cc1a6ee6 # main
