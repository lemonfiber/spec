# Keep this repo's labels in sync with the canonical set in the spec (OPS-R12).
name: labels
on:
  workflow_dispatch:
  schedule:
    - cron: "0 4 * * 1"
permissions:
  contents: read
jobs:
  sync:
    permissions:
      contents: read
      issues: write
    uses: lemonfiber/spec/.github/workflows/label-sync.yml@581a5e45d4b4f528014fa9bd6ae065b7e702a1ba # main
