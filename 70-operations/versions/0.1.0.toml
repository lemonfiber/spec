# The bootstrap release — the baseline the train governs from. Goals are the
# M2/M3 requirements that ship in 0.1.0 and are provable through both gate halves
# (cited in a merged commit AND marked ✅ in IMPLEMENTATION-STATUS). The set grows
# as the tracker's ✅ rows name more of the IDs they satisfy.
version = "0.1.0"
epoch   = "v1"
milestone = "M2"   # Core: manifest, compose driver, CLI
delivers  = "Core: manifest, compose driver, CLI"
status  = "released"
repos   = ["lemonfiber", "media-stack"]
goals   = ["B1-R4", "B1-R5", "C1-R3", "C1-R4", "C1-R5", "C1-R6", "C1-R7", "C1-R8", "C1-R9"]

[pins]
media-stack = "fbdafe0eb229c5c5016decf00b8a460b488a4225"
